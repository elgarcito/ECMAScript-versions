//we use it to replace text

const string ='Javascript it s a wonderful programming language'

const replaceString=string.replace("Javascript","TypeScript");

console.log(string);
console.log(replaceString);

//we have to take care of all capitalization because it will not
//replace it